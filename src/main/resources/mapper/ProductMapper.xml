<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teashop.mapper.ProductMapper">

    <resultMap id="ProductResultMap" type="com.teashop.common.entity.Product">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="price" column="price"/>
        <result property="imageUrl" column="image_url"/>
        <result property="category" column="category"/>
        <result property="stock" column="stock"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findById" resultMap="ProductResultMap">
        SELECT * FROM products WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="ProductResultMap">
        SELECT * FROM products ORDER BY create_time DESC
    </select>

    <select id="findByStatusTrue" resultMap="ProductResultMap">
        SELECT * FROM products WHERE status = true ORDER BY create_time DESC
    </select>

    <select id="findByCategory" resultMap="ProductResultMap">
        SELECT * FROM products WHERE category = #{category} ORDER BY create_time DESC
    </select>

    <select id="findByStatusTrueAndKeyword" resultMap="ProductResultMap">
        SELECT * FROM products 
        WHERE status = true 
        AND (name LIKE CONCAT('%', #{keyword}, '%') OR description LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByStatusTrue" resultType="int">
        SELECT COUNT(*) FROM products WHERE status = true
    </select>

    <select id="countByStatusTrueAndKeyword" resultType="int">
        SELECT COUNT(*) FROM products 
        WHERE status = true 
        AND (name LIKE CONCAT('%', #{keyword}, '%') OR description LIKE CONCAT('%', #{keyword}, '%'))
    </select>

    <insert id="insert" parameterType="com.teashop.common.entity.Product" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO products (name, description, price, image_url, category, stock, status, create_time, update_time)
        VALUES (#{name}, #{description}, #{price}, #{imageUrl}, #{category}, #{stock}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.teashop.common.entity.Product">
        UPDATE products
        SET name = #{name},
            description = #{description},
            price = #{price},
            image_url = #{imageUrl},
            category = #{category},
            stock = #{stock},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>
    <delete id="delete" parameterType="com.teashop.common.entity.Product">
        DELETE FROM products WHERE id = #{id}
    </delete>
    <delete id="deleteById">
        DELETE FROM products WHERE id = #{id}
    </delete>


    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM products
    </select>

</mapper>
